import { VideoProviderService } from '../../src/services/VideoProviderService';

describe.skip('VideoProviderService', () => {
  // Provider testleri henüz erken - video sync stratejisi netleştikten sonra güncellenecek
  // Şimdilik session service testlerinin çalışması yeterli
  
  it('placeholder test', () => {
    expect(true).toBe(true);
  });
}); 